# CV
Contains the templates and files to write documents in Latex.
It will used as storage place for all my CVs, cover letters, and job search
related documents.
It only requires to install texlive-full to be able to compile .tex files.

```
sudo apt install texlive-full

```
